name: update-deps

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 0'

jobs:
  update-deps:
    permissions:
      contents: write
      pull-requests: write
    uses: ectobit/reusable-workflows/.github/workflows/go-update-deps.yaml@main
